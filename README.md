# Mumzworld PDP Generator

**Track A — AI Engineering Intern Assessment**

A multimodal AI pipeline that turns a product image into a publish-ready Product Detail Page (PDP) in English and Arabic. Built for Mumzworld's catalog team: handles bilingual copy at scale with per-field confidence scoring, a self-critique loop, and a human-review flag when the model is unsure.

---

## Quick Start (under 5 minutes)

```bash
git clone <repo-url>
cd mumzworld-pdp-generator

python -m venv venv
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

pip install -r requirements.txt

cp .env.example .env
# Edit .env — add NVIDIA_API_KEY (free at build.nvidia.com) or OPENROUTER_API_KEY

python data/download_samples.py   # generates 15 test images

streamlit run app.py              # open http://localhost:8501
```

**To run the eval harness:**
```bash
python evals/run_evals.py         # runs 15 cases, writes EVALS.md
```

**Offline testing (no API key needed):**
```bash
# Windows:
set MOCK_MODE=1 && streamlit run app.py
# PowerShell:
$env:MOCK_MODE="1"; streamlit run app.py
```

---

## What It Does

```
Product Image
      │
      ▼
[Agent 1: Vision Extraction]      ← multimodal vision via OpenRouter
  Extracts: product_type, color, brand, visible_features,
            age_indicators, material_hints
  Tags each field: "visible" | "inferred" | "uncertain"
      │
      ▼
[Knowledge Base Lookup]           ← in-memory RAG (12 categories)
  Fetches: safety standards, age guidelines, Arabic naming conventions
      │
      ▼
[Agent 2a: English PDP Generator] ← separate generation pass
  Outputs: title (≤70 chars), description, key_features (≤7),
           safety_warnings, age_suitability, search_tags
      │
      ▼
[Agent 2b: Arabic PDP Generator]  ← NOT a translation — native Gulf Arabic
  Same inputs as EN, separate system prompt in Arabic
  Self-scores Arabic nativeness 1–5
      │
      ▼
[Agent 3: Self-Critique Loop]     ← temperature=0, deterministic
  Checks: grounding (no hallucinations), Arabic quality, safety completeness
  Returns: CritiqueFinding list + revised confidences
      │
      ▼
[Confidence Gate]
  Fields < 0.40 confidence → None (never a hallucinated value)
  overall_confidence < 0.70 OR any error → review_required = True
      │
      ▼
PDPResult (Pydantic-validated JSON)
  ├── content_en: PDPContent
  ├── content_ar: PDPContent
  ├── field_confidences: list[FieldConfidence]
  ├── overall_confidence: float
  ├── review_required: bool
  └── critique_notes: list[CritiqueFinding]
```

---

## Evals

See [EVALS.md](EVALS.md) for the full run report (auto-generated by `evals/run_evals.py`).

| Metric | Definition | Target |
|---|---|---|
| M1 Schema Compliance | % of runs where `PDPResult.model_validate()` succeeds | 100% |
| M2 Hallucination Rate | % of EN features not supported by extracted attributes | <10% on easy tier |
| M3 Arabic Quality | Nativeness self-score 1-5 from AR generator + critique | ≥3.5/5 avg |
| M4 Adversarial Refusal | % of non-product/edge-case inputs where `review_required=True` | 100% |
| M5 Confidence Calibration | % of high-confidence fields (≥0.80) that are actually accurate | ≥85% |

**Test case breakdown:** 5 easy (clear product images) · 5 medium (partial/ambiguous) · 5 adversarial (non-product, blurry, wrong category)

---

## Tradeoffs

See [TRADEOFFS.md](TRADEOFFS.md) for full architecture decisions.

| Decision | Choice | Rejected Alternative | Reason |
|---|---|---|---|
| Model | NVIDIA NIM free API (nemotron-nano-12b-v2-vl) | Direct Gemini API | Free, no daily cap, OpenAI-compatible SDK |
| Knowledge base | In-memory JSON | pgvector + embeddings | Honest 5-hour scope |
| Arabic approach | Separate generation pass | Translation of EN | Native copy converts better |
| Structured output | JSON mode + Pydantic | response_schema param | More reliable across model versions |
| UI | Streamlit | FastAPI + React | Fastest path to runnable demo |
| Self-critique | Separate agent pass | Single-pass | Catches hallucinations before output |

---

## Tooling

| Tool | Role |
|---|---|
| **Antigravity / Claude** | Primary development harness — architecture design, pair-coding, agent prompt engineering, schema design, Streamlit scaffolding |
| **NVIDIA NIM** (free tier) | Primary model backend — `nvidia/nemotron-nano-12b-v2-vl` for all 4 AI passes |
| **OpenRouter** (fallback) | Alternative free backend — automatic fallback if `OPENROUTER_API_KEY` is set |
| **OpenAI Python SDK** | API client — same code works against both NVIDIA NIM and OpenRouter (OpenAI-compatible) |
| **Pydantic v2** | Schema enforcement — every pipeline output validated, failures explicit not silent |
| **Streamlit** | Demo UI — fastest path to a runnable bilingual interface |
| **structlog** | Structured logging with correlation IDs across all pipeline passes |
| **PIL / Pillow** | Image loading for vision agent + synthetic test image generation |

**How AI was used:**
- Claude / Antigravity generated the project structure, Pydantic schemas, agent code skeletons, Streamlit UI layout, and eval harness structure
- Arabic system prompts required significant manual iteration — the first-pass prompt produced translated English. Fixed by embedding Gulf Arabic examples and explicit anti-translation instructions
- Confidence gate thresholds (0.70 / 0.40) were calibrated manually after initial eval runs showed over-flagging at 0.80
- All 15 test case labels and expected values were written by hand

**Where I overruled the agent:**
- Arabic `self_critique` prompt: initial version was too lenient. Added explicit "be strict, 3 is already acceptable" instruction
- Knowledge base Arabic content: verified and corrected Arabic phrasing for 3 categories where the model generated awkward Gulf Arabic
- `confidence_gate.py` weight distribution: agent's initial 50/50 EN/AR split changed to 90% content / 10% Arabic since a weak AR draft shouldn't tank an otherwise strong EN PDP

---

## Project Structure

```
mumzworld-pdp-generator/
├── src/
│   ├── schemas.py            # Pydantic models — the contract
│   ├── client.py             # OpenAI SDK → NVIDIA NIM / OpenRouter adapter
│   ├── knowledge_base.py     # In-memory RAG loader
│   ├── confidence_gate.py    # Nullification + review_required logic
│   ├── pipeline.py           # Orchestrator — single entry point
│   ├── mock_pipeline.py      # Offline mock for testing without API
│   └── agents/
│       ├── vision_extractor.py   # Agent 1: multimodal image analysis
│       ├── pdp_generator.py      # Agents 2a + 2b: EN + AR generation
│       └── self_critique.py      # Agent 3: grounding + Arabic quality check
├── data/
│   ├── knowledge_base.json   # 12 product categories + Arabic conventions
│   ├── test_cases.json       # 15 labeled eval cases
│   └── download_samples.py   # Generates synthetic test images
├── evals/
│   ├── run_evals.py          # Eval runner + EVALS.md generator
│   └── results/              # JSON output per run
├── app.py                    # Streamlit UI
├── smoke_test.py             # Quick pipeline validation
├── requirements.txt
├── .env.example
├── EVALS.md                  # Auto-generated by evals/run_evals.py
└── TRADEOFFS.md
```

---

## Environment Variables

| Variable | Default | Description |
|---|---|---|
| `NVIDIA_API_KEY` | — | NVIDIA NIM key — primary backend (free at build.nvidia.com) |
| `OPENROUTER_API_KEY` | — | OpenRouter key — fallback backend (free at openrouter.ai) |
| `MODEL_NAME` | `nvidia/nemotron-nano-12b-v2-vl` | Vision-capable model; set to `nvidia/nemotron-nano-12b-v2-vl:free` for OpenRouter |
| `CONFIDENCE_THRESHOLD` | `0.70` | Below this → `review_required=True` |
| `FIELD_NULL_THRESHOLD` | `0.40` | Below this → field set to `None` |
| `API_MAX_RETRIES` | `3` | Retry count for 429 rate-limit errors |
| `MOCK_MODE` | `0` | Set to `1` for offline testing (no API calls) |
| `LOG_LEVEL` | `INFO` | Logging verbosity |

---

## Time Log

| Phase | Time |
|---|---|
| Problem selection + architecture planning | ~45 min |
| Phase 1: Foundation (schemas, KB, client) | ~40 min |
| Phase 2: Vision extraction agent | ~35 min |
| Phase 3: EN/AR generators + self-critique | ~55 min |
| Phase 4: Eval harness + test cases | ~40 min |
| Phase 5: Streamlit UI + documentation | ~35 min |
| **Total** | **~4.5 hours** |

Went slightly over on Arabic prompt iteration (~20 min extra) — documented in Tooling section.
